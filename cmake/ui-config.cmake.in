@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(WIN32)
    find_dependency(unofficial-webview2 CONFIG)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/ui-targets.cmake")

check_required_components(ui)